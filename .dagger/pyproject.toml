[project]
name = "satvu-api-sdk-ci"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["dagger-io>=0.21.8", "toml>=0.10.2"]

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
