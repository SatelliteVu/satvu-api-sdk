{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    [
      "@semantic-release/release-notes-generator",
      {
        "writerOpts": {
          "commitPartial": "* {{#if scope}}**{{scope}}:** {{/if}}{{subject}}\n"
        }
      }
    ],
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "sed -i 's/^version = \".*\"/version = \"${nextRelease.version}\"/' pyproject.toml && uv lock && dagger call build-release --is-qa=false --triggered-api=none --spec-env=prod export --path=./build-output --wipe && mv ./build-output/dist ./dist && mv ./build-output/.cache ./.cache && rsync -a --delete --exclude='__pycache__/' ./build-output/src/satvu/services/ src/satvu/services/ && rm -rf ./build-output && git add src/satvu/services/"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "pyproject.toml",
          "uv.lock",
          "src/satvu/services/**",
          "!src/satvu/services/**/test_schemas.py"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]"
      }
    ],
    "@semantic-release/github"
  ]
}
